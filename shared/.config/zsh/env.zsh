# env.zsh

# Add .local/bin to the PATH
export PATH="$HOME/.local/bin:$PATH"

# DXVK global config, better copy to a specific app and set it there.
export DXVK_CONFIG_FILE="$HOME/.config/dxvk/dxvk.conf"
